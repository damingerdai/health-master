apiVersion: v1
kind: Service
metadata:
  name: {{ include "health-master.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "health-master.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "health-master.selectorLabels" . | nindent 4 }}
